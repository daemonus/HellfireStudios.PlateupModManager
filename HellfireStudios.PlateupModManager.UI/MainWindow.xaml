<Window x:Class="HellfireStudios.PlateupModManager.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HellfireStudios.PlateupModManager.UI"
        xmlns:vm="clr-namespace:HellfireStudios.PlateupModManager.UI.ViewModels"
        xmlns:views="clr-namespace:HellfireStudios.PlateupModManager.UI.Views"
        mc:Ignorable="d"
        Title="PlateUp! Mod Manager" Height="700" Width="1050"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e2e">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:WorkshopBrowserViewModel}">
            <views:WorkshopBrowserView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:InstalledModsViewModel}">
            <views:InstalledModsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProfilesViewModel}">
            <views:ProfilesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Grid.RowSpan="2" Background="#181825" BorderBrush="#313244" BorderThickness="0,0,1,0">
            <DockPanel>
                <!-- App Title -->
                <StackPanel DockPanel.Dock="Top" Margin="16,20,16,24">
                    <TextBlock Text="🍽️ PlateUp!" FontSize="22" FontWeight="Bold" Foreground="#cdd6f4" />
                    <TextBlock Text="Mod Manager" FontSize="13" Foreground="#a6adc8" Margin="0,2,0,0" />
                </StackPanel>

                <!-- Game Status -->
                <Border DockPanel.Dock="Top" Background="#11111b" CornerRadius="8" Padding="12,8" Margin="12,0,12,16">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#f38ba8" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGameRunning}" Value="True">
                                            <Setter Property="Fill" Value="#a6e3a1" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Foreground="#bac2de" FontSize="12">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Game Not Running" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGameRunning}" Value="True">
                                            <Setter Property="Text" Value="Game Running" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Navigation Buttons -->
                <StackPanel DockPanel.Dock="Top" Margin="8,0">
                    <Button Content="📦  Installed Mods" Command="{Binding NavigateToInstalledCommand}"
                            Style="{StaticResource NavButtonStyle}" />
                    <Button Content="🔍  Workshop Browser" Command="{Binding NavigateToWorkshopCommand}"
                            Style="{StaticResource NavButtonStyle}" />
                    <Button Content="📋  Profiles" Command="{Binding NavigateToProfilesCommand}"
                            Style="{StaticResource NavButtonStyle}" />
                    <Button Content="⚙️  Settings" Command="{Binding NavigateToSettingsCommand}"
                            Style="{StaticResource NavButtonStyle}" />
                </StackPanel>

                <!-- Bottom spacer -->
                <StackPanel DockPanel.Dock="Bottom" />
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <ContentControl Grid.Column="1" Grid.Row="0"
                        Content="{Binding CurrentView}"
                        Margin="0" />

        <!-- Status Bar -->
        <Border Grid.Column="1" Grid.Row="1" Background="#181825" BorderBrush="#313244" BorderThickness="0,1,0,0"
                Padding="16,8">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#a6adc8" FontSize="12" />
        </Border>
    </Grid>
</Window>
